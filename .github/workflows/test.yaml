name: CI DIAGNOSTIC

on:
  push:
    branches:
      - '**'
  workflow_dispatch:

jobs:
  ci-started:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "CI TRIGGERED"
          echo "REF=$GITHUB_REF"
          echo "EVENT=$GITHUB_EVENT_NAME"

  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "BUILD JOB RUNNING"

swaggerServer:
  enable: true
  port: 1314

apiServer:  
  enable: true
  port: 8081

postgreSQL:
  host: localhost
  port: 5432
  user: postgres
  password: postgres
  dbname: wallet
  sslmode: disable

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0
  maxRetries: 3
  poolSize: 10

services:
  transaction:
    baseURL: "http://localhost:8082"